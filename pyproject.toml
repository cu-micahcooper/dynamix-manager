[build-system]
requires = ["setuptools>=65.5.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dynamix-manager"
version = "0.1.0"
description = "Phase-0 support for the TeamDynamix executive analytics workspace."
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }

dependencies = [
    "duckdb",
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
    "jupyterlab",
    "nbformat",
    "pandas",
    "plotly",
    "python-dotenv",
    "requests",
]

[project.optional-dependencies]
plugin = ["mcp>=1.26,<2"]
hosted = ["mcp>=1.30,<2", "PyJWT[crypto]>=2.12,<3", "cryptography>=46,<48", "httpx>=0.27,<1", "uvicorn>=0.30,<1"]
dev = [
    "nbclient",
    "pytest",
    "ruff",
]

[project.scripts]
dynamix-manager = "dynamix_manager.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["dynamix_manager*"]

[tool.setuptools.package-data]
"dynamix_manager" = ["plugin_app.html"]
