[build-system]
requires = ["setuptools>=65.5.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dynamix-manager"
version = "0.1.0"
description = "Phase-0 support for the TeamDynamix executive analytics workspace."
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }

dependencies = [
    "duckdb",
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
    "jupyterlab",
    "nbformat",
    "pandas",
    "plotly",
    "python-dotenv",
    "requests",
]

[project.optional-dependencies]
hosted = ["mcp>=1.30,<2", "PyJWT[crypto]>=2.12,<3", "cryptography>=46,<48", "httpx>=0.27,<1", "uvicorn>=0.30,<1", "pypdf>=5,<7"]
web = ["bleach>=6.4,<7", "fastapi>=0.115,<1", "uvicorn>=0.30,<1", "httpx>=0.27,<1", "openai>=2,<4"]
dev = [
    "nbclient",
    "pytest",
    "ruff",
]

[project.scripts]
dynamix-manager = "dynamix_manager.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["dynamix_manager*"]

[tool.setuptools.package-data]
"dynamix_manager" = ["plugin_app.html"]
"dynamix_manager.workbench" = ["static/*.html", "static/*.css", "static/*.js"]
